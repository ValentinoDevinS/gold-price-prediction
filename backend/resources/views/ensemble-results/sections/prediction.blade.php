<x-ui.card>

    <x-ui.section-header
        title="Prediction Result"
        description="Predicted gold price generated by the ensemble algorithm."
    />

    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">

        <x-ui.stat-card
            title="Predicted Gold Price"
            :value="'$' . number_format(
                $ensembleResult->predicted_price,
                2
            ) . ' USD/oz'"
            description="Final predicted gold price."
        />

        <x-ui.stat-card
            title="Prediction Date"
            :value="$ensembleResult->prediction_date?->format('Y-m-d')"
            description="Target date of the prediction."
        />

        <div class="bg-white rounded-lg border p-6">

            <div class="text-sm text-gray-500">
                Ensemble Method
            </div>

            <div class="mt-2">

                <x-ui.badge
                    :variant="$ensembleResult->isAverage()
                        ? 'success'
                        : ($ensembleResult->isWeighted()
                            ? 'primary'
                            : ($ensembleResult->isMedian()
                                ? 'warning'
                                : 'secondary'))"
                >
                    {{ str_replace('_', ' ', $ensembleResult->ensemble_method) }}
                </x-ui.badge>

            </div>

            <p class="text-sm text-gray-500 mt-3">
                Algorithm used to combine multiple prediction models.
            </p>

        </div>

    </div>

</x-ui.card>